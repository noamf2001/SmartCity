from Building import Section
from Building import Point

def split_to_sections(points_list) -> list:
    """

    :param start_point:
    :param end_point:

    :return list of sections, so that every section is one kind of description
    """
